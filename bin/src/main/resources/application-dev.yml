spring:
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5432}/gadgets_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    com.entreprise.gadgets: DEBUG
