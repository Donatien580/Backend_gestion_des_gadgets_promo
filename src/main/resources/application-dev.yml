spring:
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5433}/gadgets_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    com.entreprise.gadgets: DEBUG

